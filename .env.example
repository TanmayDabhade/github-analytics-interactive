# GitHub OAuth app credentials
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
# Optional override for the redirect URI (defaults to http://localhost:5173/)
GITHUB_REDIRECT_URI=
# Port for the OAuth helper server (defaults to 5174)
SERVER_PORT=
